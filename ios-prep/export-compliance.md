# Export Compliance

Current app version:
- does not implement custom cryptography;
- uses standard HTTPS/WebSocket transport provided by iOS/WebKit and Node hosting;
- does not include VPN, encrypted messaging, custom crypto, DRM or secure file storage.

In App Store Connect, the typical answer is that the app uses only standard encryption exempt from export compliance.
Verify current Apple wording at submission time.
